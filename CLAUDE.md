# CLAUDE.md

쿼터뷰 싱글 로그라이트 던전 크롤 프로젝트. 자동 전투 + 가챠 조합 빌드.

- **시뮬레이션 코어**: C++ (static lib) — 클라이언트·서버가 공유
- **클라이언트 프레젠테이션**: Unity / C# / URP
- **서버**: C# / ASP.NET Core — 검증 시에만 C++ 코어를 P/Invoke, MySQL

**C++는 결정론 코어에만 쓴다.** 네트워킹·DB·툴링은 생산성이 높은 쪽을 고른다.
C++를 쓰는 이유는 언어 선호가 아니라 "서버와 클라이언트가 물리적으로 동일한 시뮬레이션
바이너리를 돌려야 한다"는 요구뿐이다. 그 요구가 없는 영역까지 C++로 끌고 가지 말 것.

상세 설계는 `docs/design.md` 참조. 게임 로직·데이터 구조 작업 전에 반드시 먼저 읽을 것.

---

## 결정론 규칙 (최우선 — 위반 시 즉시 지적하고 작업 중단)

시뮬레이션 레이어는 **같은 입력 → 항상 같은 출력**을 보장해야 한다.
서버가 클라이언트의 런을 재실행해 검증하므로, 아래 규칙은 예외 없이 적용된다.

**적용 범위: C++ 시뮬레이션 코어에 한한다.** Unity C# 프레젠테이션 계층은 대상이 아니다
(C#는 시뮬 상태를 읽어 그리기만 하므로 float를 써도 결과에 영향이 없다).

### 수치

- `float` / `double` 사용 금지. 좌표·스탯·시간 전부 `Fixed`(고정소수점)
- 시뮬 상태와 해시 입력에 `int` / `long` / `size_t` 등 **가변폭 정수 금지**. `int32_t` 등 고정폭만 사용
- 부호 있는 정수 오버플로는 UB다. `Fixed` 곱셈에서 실제로 밟게 되므로 넓은 타입으로 승격 후 시프트
- 음수의 우측 시프트는 C++20 이전 구현 정의다. 시프트·반올림 처리는 `fixed.h`의 헬퍼 하나로 격리하고 단위 테스트로 동작을 고정할 것
- 나눗셈은 반올림 방향을 명시적으로 고정 (C++11부터 0방향 절삭으로 표준화되었으나 의도를 코드에 남긴다)

### 컨테이너 · 순서

- `std::unordered_map` / `unordered_set` **순회** 금지 (조회는 허용)
- **`std::hash` 사용 금지.** 구현체마다 결과가 다르다 → 서버-클라 불일치의 직행 경로.
  해시가 필요하면 자체 FNV-1a 또는 SplitMix64 finalizer를 쓸 것
- 포인터 값·메모리 주소를 정렬 키나 해시 입력으로 사용 금지
- 정렬: 비교 함수가 **완전 순서(total order)** 를 만들면 `std::sort` 허용(더 빠름).
  동등 원소가 존재할 수 있으면 `std::stable_sort`. `sourceId`가 유일하다면 전자를 쓴다
- 시스템 실행 순서 고정. 병렬화 시에도 결과 병합 순서를 고정할 것
- 엔티티 삭제는 틱 중간에 하지 말고 틱 종료 시 일괄 압축

### 시간 · 난수

- `std::chrono`, 프레임 delta 등 실시간 참조 금지. 시간은 **틱 카운터**로만 표현
- 전역 RNG 금지. `World`의 마스터 시드에서 파생된 스트림만 사용

### 빌드 · 검증

- `-ffast-math` 금지
- 시뮬 코어는 **UBSan / ASan 빌드에서 테스트를 통과해야 한다.** UB 하나가 곧 검증 실패다
- 시뮬 결과는 컴파일러·최적화 레벨·OS에 무관해야 한다.
  **Debug 빌드와 Release 빌드의 체크섬 일치를 CI에서 검증할 것**

## 레이어 분리

```
              [C++ 시뮬레이션 코어]  고정 20Hz, 결정론, static lib
                          │
                 C ABI (extern "C") — 하나를 양쪽이 재사용
                    ┌─────┴─────┐
     상태 버퍼 + EventQueue   headless run → checksum
                    ▼           ▼
        [Unity 클라이언트]   [C# 서버]  ASP.NET Core, MySQL
         C# / URP            리플레이 검증 · 리더보드
```

- 프레젠테이션 → 시뮬레이션 **역방향 참조 금지.** C#는 시뮬 상태를 **읽기만** 한다
- 시뮬레이션 코드에 렌더링·입력·오디오 헤더 include 금지. Unity API 참조 금지
- 시뮬 코어는 독립 CMake 타깃으로 빌드되며 Unity·서버 어느 쪽에도 의존하지 않는다.
  **리뷰가 아니라 빌드 단위로 강제한다**
- C++ → C# 경계는 **C ABI(`extern "C"`)만.** C++ 예외·STL 타입·가상 함수 노출 금지.
  클라이언트용 표면(상태 버퍼 + 이벤트)과 서버용 표면(headless run → checksum)은
  **같은 ABI의 두 진입점**이다. 코어를 두 번 구현하지 않는다
- 히트스톱·슬로모·배속은 렌더 재생 속도와 틱 소비 속도만 조작.
  시뮬레이션 틱 내부 계산은 항상 고정 속도로 진행

## 코드 방침

- 생각하고 나서 코딩할 것. 단순한 해법 우선
- 요청 범위 밖을 건드리지 말 것 (surgical changes)
- 아이템·조합식·스킬·이벤트·웨이브·해금 조건은 **전부 데이터 주도**. 하드코딩 분기 금지
- 새 던전 기믹은 코어에 `if` 추가가 아니라 기존 모디파이어 조합으로 표현할 것
- 성능 관련 변경은 추측 말고 측정 후 진행

## 테스트

- 시뮬레이션 관련 변경 시 재현성 테스트를 반드시 통과할 것
  (같은 시드 N회 실행 → 매 틱 `checksum()` 일치)
- 실패 시 **최초로 갈라진 틱 번호**를 보고할 것
- 시스템별 부분 체크섬도 함께 기록한다 → 어느 시스템에서 갈라졌는지 즉시 특정

## Git

- 커밋 메시지는 한국어로 작성
